<h1>Pages#home</h1>

<div class="container">
  <div class="row">
    <% @products.each do |product| %>
      <div class="col-xs-12 col-sm-6 col-md-4">
        <%= link_to product_path(product), class: "btn" do %>

          <%= product.name %>
          <%= product.stock %>
          <%= product.price/100.to_f %>€
          <p class="btn btn-danger btn-block">Ajouter au panier</p>
        <% end %>
      </div>
    <% end %>
  </div>
</div>
