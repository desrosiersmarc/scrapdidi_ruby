<div class="sous-menu-min-height">
  <div class="container">
    <h2>Scrapbooking</h2>
      <div class="row">
          <% @families.each do |family| %>
        <div class="col-xs-12 col-sm-6 col-lg-3">
            <p class="sous-menu-btn">
              <%= link_to root_path do %>
                <%= family.name %>
              <% end %>
            </p>
        </div>
          <% end %>
    </div>
  </div>
</div>
